# MemCanvas taxonomy

This directory contains released labels used to merge five QA datasets into the paper-level topic and evidence taxonomies.

- `topic_labels.txt`: `(dataset, split:index, major_topic, subtopic)`
- `modality_labels.txt`: `(dataset, split:index, modality, hop_type)`

See `../../docs/dataset_taxonomy.md` for details.
